import React from 'react';
import classes from "./Results.module.css";

const Results = (props) => {
    return (
        <div>
             Results Page
        </div>
    );
};

export default Results;
